module.exports = {
  printWidth: 80, // Max line length before wrapping
  tabWidth: 2, // Number of spaces per tab
  useTabs: false, // Use spaces instead of tabs
  semi: true, // Always add semicolons
  singleQuote: true, // Use single quotes instead of double
  trailingComma: "all", // Add trailing commas where possible
  bracketSpacing: true, // Spaces between brackets `{ foo: bar }`
  arrowParens: "always", // Always include parentheses in arrow functions
  endOfLine: "lf", // Consistent line endings
  proseWrap: "preserve", // Keep markdown wrapping as is
};
